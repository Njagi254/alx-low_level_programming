#include<stdio.h>

/**
 * main - entry point
 *
 *  Description : Program that prints all possible combinations
 *  of 3 digits follwed by comma then space
 *
 * return: Alasy 0 (Success)
 */
int main(void);

int (i);
int (e);
int (g);
{
for (i =  48; i <=  57; i++)
	{
	for (e = 49; e <= 57; e++)
		{
		for (g = 50; g <= 57; g++)
			{
			if (e > i && g > e)
			{
			putchar(i);
			putchar(e);
			putchar(g);
			}
			if (i != 56; || e != 57)
			{
				putchar(44);
				putchar(32);
			}
putchar(10);
			}
		}
return (0);
	}
}
/**return (0);
}*/
